# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environment (managed by uv; uv.lock is committed for reproducibility)
.venv/

# Node / frontend build (package-lock.json IS committed for reproducibility)
frontend/node_modules/
# Vite build output, served by the backend; rebuild with `npm --prefix frontend run build`
src/solopm/web/dist/

# Local SoloPM store, if ever placed inside the repo (default lives in ~/.solopm)
.solopm/

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
