{
  "name": "solopm-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SoloPM web app — TypeScript + Vite client of the local backend.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "happy-dom": "^20.10.6",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
